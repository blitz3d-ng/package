if(BB_DESKTOP)
  bb_start_module(graphics.gl)
  set(DEPENDS_ON bb.graphics)
  set(SOURCES graphics.gl.cpp)
  set(LIBS glew)
  IF(BB_WINDOWS)
    set(LIBS ${LIBS} opengl32)
  ELSEIF(BB_MACOS)
    set (LIBS ${LIBS} "-framework OpenGL")
  ELSE(BB_LINUX)
    set(LIBS ${LIBS} GL GLX X11 Xrandr Xinerama Xcursor pthread)
  ENDIF()
  bb_end_module()

  if(BB_MACOS)
    target_compile_definitions(bb.graphics.gl PUBLIC -DGL_SILENCE_DEPRECATION)
  endif()
endif()
