IF(NOT BB_EMSCRIPTEN)
  bb_start_module(sockets)
  set(DEPENDS_ON bb.stream)
  set(SOURCES sockets.cpp sockets.h)
  if(BB_WINDOWS)
    set(SYSTEM_LIBS wsock32)
  elseif(BB_NX)
    set(SYSTEM_LIBS nx)
  endif()
  bb_end_module()
ENDIF()
