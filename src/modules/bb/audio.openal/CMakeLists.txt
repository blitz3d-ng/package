find_package(OpenAL)
if(OPENAL_FOUND)
  bb_start_module(audio.openal)
  set(DEPENDS_ON bb.audio)
  set(SOURCES audio.openal.h audio.openal.cpp commands.h)
  set(SYSTEM_LIBS ${OPENAL_LIBRARY})
  bb_end_module()
endif()
