if(BB_IOS)
  bb_start_module(system.ios)
  set(DEPENDS_ON bb.system)
  set(SOURCES system.ios.cpp system.ios.h system.ios.m)
  set(SYSTEM_LIBS "-framework CoreGraphics" "-framework UIKit")
  bb_end_module()
endif()
