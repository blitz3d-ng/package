name: Windows System
description: Provides access to various Windows operating system functions.

platforms:
  - windows

dependencies:
  - system
  - bank

commands:
  - CallDLL( dll_name$,func_name$,in_bank%=0,out_bank%=0 ):"bbCallDLL"
