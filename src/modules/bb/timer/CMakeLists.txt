bb_start_module(timer)
set(DEPENDS_ON bb.runtime)
set(SOURCES timer.cpp timer.h)
if(BB_WINDOWS)
  set(SOURCES ${SOURCES} timer.windows.cpp)
  set(LIBS winmm)
else()
  set(SOURCES ${SOURCES} timer.noop.cpp)
endif()
bb_end_module()
