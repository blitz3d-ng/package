if(TARGET SDL2)
  bb_start_module(graphics.sdl)
  set(DEPENDS_ON bb.graphics.gl)
  set(SOURCES graphics.sdl.cpp graphics.sdl.h)
  set(LIBS SDL2)
  if(BB_WINDOWS)
    set(SYSTEM_LIBS setupapi version imm32)
  endif()
  if(BB_ANDROID)
    set(DEPENDS_ON ${DEPENDS_ON} bb.context.egl)
  endif()
  bb_end_module()
endif()
