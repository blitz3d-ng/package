if(BB_MOBILE)
  bb_start_module(runtime.glfm)
  set(DEPENDS_ON bb.runtime bb.system bb.input bb.graphics bb.blitz3d.gles2)
  set(SOURCES runtime.glfm.cpp runtime.glfm.h main.cpp file_compat.h)
  set(SOURCES sample.cpp ${SOURCES})
  set(LIBS glfm)
  IF(BB_IOS)
    set(LIBS ${LIBS} "-framework OpenGLES" "-framework UIKit" "-framework CoreGraphics" "-framework QuartzCore" "-framework Foundation")
  ENDIF()

  bb_end_module()
endif()
