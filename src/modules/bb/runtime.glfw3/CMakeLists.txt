if(BB_DESKTOP)
  bb_start_module(runtime.glfw3)
  set(DEPENDS_ON bb.runtime bb.input bb.pixmap bb.blitz2d bb.graphics bb.blitz3d bb.blitz3d.gl bb.timer)
  set(SOURCES runtime.glfw3.cpp runtime.glfw3.h scancodes.cpp)
  set(LIBS glfw3)
  if(BB_LINUX)
    set(LIBS ${LIBS} dl)
  elseif(BB_MACOS)
    set(SYSTEM_LIBS "-framework Cocoa;-framework IOKit;-framework CoreFoundation;-framework CoreVideo")
  elseif(BB_WINDOWS)
    set(LIBS ${LIBS} dxguid)
  endif()
  bb_end_module()
endif()
