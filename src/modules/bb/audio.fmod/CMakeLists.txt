IF(BB_WINDOWS)
  add_library (bb.audio.fmod STATIC audio.fmod.h sound.cpp sound.h driver.cpp driver.h)
  target_include_directories(bb.audio.fmod PRIVATE ../../../../common/include)
  target_link_libraries (bb.audio.fmod bb.audio bb.runtime fmodvc)
ENDIF()
