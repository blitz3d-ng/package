set(GLFW_BUILD_EXAMPLES off CACHE BOOLEAN "" FORCE)
set(GLFW_BUILD_TESTS off CACHE BOOLEAN "" FORCE)
set(GLFW_BUILD_DOCS off CACHE BOOLEAN "" FORCE)
set(GLFW_INSTALL off CACHE BOOLEAN "" FORCE)

IF (BB_MACOS)
  set(GLFW_USE_CHDIR off CACHE BOOLEAN "" FORCE)
ENDIF()

add_subdirectory(src)

target_compile_definitions(glfw PRIVATE GLEW_STATIC)
